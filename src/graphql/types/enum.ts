export default `
    enum RoomType {
        public
        community
        private
    }

    enum Gender { 
        boy
        girl
    }

    enum AudienceRole {
        speaker
        audience
        owner
    }

    enum RoomStatus {
        open
        live
        close
    }
    
    enum RoomMode {
        question
        pairing
        chaseImage
    }

    enum FollowAction  {
        follow
        unfollow
    }
`
